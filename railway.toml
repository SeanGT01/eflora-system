[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 --access-logfile - --error-logfile - run:app"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyMaxRetries = 5
