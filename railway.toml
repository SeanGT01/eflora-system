[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "flask db upgrade || echo 'Migration warning - continuing'; gunicorn --bind 0.0.0.0:$PORT run:app"
healthcheckPath = "/"
healthcheckTimeout = 300
